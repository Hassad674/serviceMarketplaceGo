// `FeePreview` lives in `@/shared/components/billing/fee-preview` (P9
// — consumed cross-feature by proposal). Re-exported here for
// back-compat.
export { FeePreview } from "@/shared/components/billing/fee-preview"
