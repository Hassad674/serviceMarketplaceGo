// Billing types live in `@/shared/types/billing` (P9 — `FeePreview` UX
// is consumed cross-feature by proposal). Re-exported here for back-compat.
export type {
  FeePreviewRole,
  FeePreviewTier,
  FeePreview,
} from "@/shared/types/billing"
