// `useFeePreview` lives in `@/shared/hooks/billing/use-fee-preview` (P9 —
// `FeePreview` UX is consumed cross-feature by proposal). Re-exported
// here for back-compat.
export { useFeePreview } from "@/shared/hooks/billing/use-fee-preview"
