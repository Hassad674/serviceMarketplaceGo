// `UpgradeModal` lives in `@/shared/components/subscription/upgrade-modal`
// (P9 — consumed cross-feature by proposal). Re-exported here for
// back-compat.
export { UpgradeModal } from "@/shared/components/subscription/upgrade-modal"
