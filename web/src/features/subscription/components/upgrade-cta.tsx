// `UpgradeCta` lives in `@/shared/components/subscription/upgrade-cta`
// (P9 — consumed cross-feature by proposal). Re-exported here for
// back-compat.
export { UpgradeCta } from "@/shared/components/subscription/upgrade-cta"
