// `useCurrentMonth` lives in
// `@/shared/hooks/billing-profile/use-current-month` (P9 — consumed
// cross-feature via the shared `CurrentMonthAggregate`). Re-exported
// here for back-compat.
export { useCurrentMonth } from "@/shared/hooks/billing-profile/use-current-month"
