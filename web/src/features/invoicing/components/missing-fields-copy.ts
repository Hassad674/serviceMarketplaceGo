// `describeMissing` and friends live in
// `@/shared/lib/billing-profile/missing-fields-copy` (P9 — used by the
// shared `BillingProfileCompletionModal`). Re-exported here for
// back-compat.
export {
  describeMissing,
  fieldLabel,
  reasonLabel,
} from "@/shared/lib/billing-profile/missing-fields-copy"
