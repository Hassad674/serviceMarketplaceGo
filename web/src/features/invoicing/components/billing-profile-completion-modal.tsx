// `BillingProfileCompletionModal` lives in
// `@/shared/components/billing-profile/billing-profile-completion-modal`
// (P9 — consumed cross-feature by wallet). Re-exported here for
// back-compat.
export { BillingProfileCompletionModal } from "@/shared/components/billing-profile/billing-profile-completion-modal"
