// `ReferralSystemMessage` lives in
// `@/shared/components/referral/referral-system-message` (P9 — consumed
// cross-feature by messaging). Re-exported here for back-compat.
export {
  ReferralSystemMessage,
  type ReferralSystemMessageMetadata,
} from "@/shared/components/referral/referral-system-message"
