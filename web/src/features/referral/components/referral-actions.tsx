// `ReferralActions` lives in
// `@/shared/components/referral/referral-actions` (P9 — used by the
// shared `ReferralSystemMessage`). Re-exported here for back-compat.
export { ReferralActions } from "@/shared/components/referral/referral-actions"
