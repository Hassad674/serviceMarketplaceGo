// `ReportDialog` lives in `@/shared/components/reporting/report-dialog`
// (P9 — consumed cross-feature by messaging and job). Re-exported here
// so any code still importing from the reporting feature keeps working.
export { ReportDialog } from "@/shared/components/reporting/report-dialog"
