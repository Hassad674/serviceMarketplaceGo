// `StarRating` lives in `@/shared/components/review/star-rating` (P9
// — consumed by `ReviewModal`, which is now shared). Re-exported here
// for any direct callers.
export { StarRating } from "@/shared/components/review/star-rating"
