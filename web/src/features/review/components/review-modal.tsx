// `ReviewModal` lives in `@/shared/components/review/review-modal`
// (P9 — consumed cross-feature by messaging). Re-exported here so any
// code still importing from the review feature keeps working.
export { ReviewModal } from "@/shared/components/review/review-modal"
