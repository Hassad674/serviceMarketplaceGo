playwright-report/

# F.3.2: src/shared/types/api.d.ts is generated by `npm run generate-api`
# (or `:offline` against backend/internal/handler/testdata/openapi.golden.json)
# and is committed to the repo so reviewers can diff the API contract
# alongside backend changes. See web/CLAUDE.md for the regen flow.
