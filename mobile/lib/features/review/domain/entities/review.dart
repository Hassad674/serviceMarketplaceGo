/// Re-export of the shared Review/AverageRating entities.
///
/// The canonical home is [mobile/lib/core/models/review.dart]. This file
/// remains as a compatibility shim so existing `features/review/...` code
/// keeps compiling without touching every import site.
export '../../../../core/models/review.dart';
