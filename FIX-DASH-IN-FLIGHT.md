# FIX-DASH in flight

Bug 1 — Messaging unread count stale
Bug 2 — /stats shows "Données insuffisantes" instead of unit counts
Bug 3 — Billing info action item → 404, remove entirely

+ harden tests (vitest + e2e + mobile widget tests).
